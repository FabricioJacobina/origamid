/*
  * var nome = 'Fabrício'; //String
  * var idade = 26;  //Number
  * var possuiFaculdade = true;  //Boolean
  * var time; //Undefined
  * var comida = null; //Null  mas retornará Object
  * var simbolo = Symbol(); //Symbol
  * var novoObjeto = {}; //Object

  * console.log(typeof nome, typeof idade, typeof possuiFaculdade, typeof time, typeof comida, typeof simbolo, typeof novoObjeto);
*/

// Declare uma variável contendo uma String
var presidente = 'Bolsonaro';

// Declare uma variável contendo um número dentro de uma String
var numString = '1999';

// Declare uma variável com a sua idade
var idade = 26;

// Declare duas variáveis, uma com seu nome e outra com seu sobrenome e some as mesmas
var nome = 'Fabrício';
var sobrenome = 'Jacobina';
var nomeCompleto = nome + ' ' + sobrenome;

//Coloque a seguinte frase em uma variável: It's time
var frase = 'It\'s time';

// Verifique o tipo da variável que contém o seu nome
console.log(typeof nome);